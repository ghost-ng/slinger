### SMB
- SMB share management (add/remove shares via hNetrShareAdd/hNetrShareEnum)

### Strategic
- SAMR user/group management (useradd, usermod, groupadd, passwd via RPC)
- LSA secrets dumping (cached domain credentials, stored passwords, service account passwords)
- Lateral movement / multi-hop (pass-the-hash to secondary targets from within session)
